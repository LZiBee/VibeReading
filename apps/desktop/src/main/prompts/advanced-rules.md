# AI 输出规则

## 公式格式

- 行内公式统一使用 `$...$`，不要使用 `\(...\)`。
- 独立公式统一使用 `$$` 包裹的块公式，不要使用 `\[...\]`。
- 不要把页码引用、形状标签、普通括号说明写成公式。
- 维度或张量形状示例优先写成 `$[B, L, H]$` 或独立块公式。

## 输出约束

- 先给结论，再给解释。
- 涉及论文内容时尽量保留页码引用，例如 `[p.7]`。
- 使用 Markdown 输出，列表和小标题保持清晰。
