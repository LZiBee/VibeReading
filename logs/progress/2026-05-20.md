## 09:43 - 探索 MinerU PDF API

- 目标：确认 MinerU 是否可用于 PDF 解析，优先查官方 API 文档并实际调用一次接口，看看返回的是任务状态、下载链接还是直接的结构化解析结果。
- 状态：已完成
- 已完成：已重新阅读项目开发约定；已确认当前仓库根目录是 `E:/THESIS_AGENT`；已搜索本地代码库，暂未发现现成的 MinerU 接入实现；已确认 MinerU 官方存在免 Token 的 Agent 解析接口；已用你的 `C:\Users\27368\Desktop\mineru.txt` 读取到可用 API token，并验证了 MinerU 精准解析 API 的鉴权和批任务创建；已把解析流程沉淀为 `scripts/mineru_precise_parse.py` 和 `scripts/mineru_agent_parse.py`；已用远程 URL 模式成功拿到精准解析结果 zip，确认其中包含 `full.md`、`content_list.json`、`content_list_v2.json`、`layout.json`、`model.json`、页面原图与 `full_zip_url`。
- 修改文件：`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`、`scripts/mineru_agent_parse.py`、`scripts/mineru_precise_parse.py`、`scripts/README.md`
- 验证结果：已运行 `python scripts/mineru_agent_parse.py sample/NIPS-2012-large-scale-distributed-deep-networks-Paper.pdf --output-markdown workspace/mineru/NIPS-2012-large-scale-distributed-deep-networks-Paper.md`；已运行 `python scripts/mineru_precise_parse.py sample/NIPS-2012-large-scale-distributed-deep-networks-Paper.pdf --token-file C:\Users\27368\Desktop\mineru.txt --output-dir workspace/mineru`，本地上传模式在 OSS 域名解析处失败；已运行 `python scripts/mineru_precise_parse.py --source-url https://cdn-mineru.openxlab.org.cn/demo/example.pdf --token-file C:\Users\27368\Desktop\mineru.txt --output-dir workspace/mineru` 并成功下载结果 zip。
- 下一步：如果要在产品里实现“框选段落/图片/表格后点击跳到 Markdown 对应位置”，应优先消费精准解析输出里的 `content_list.json` / `layout.json` / 图片与页码坐标信息，而不是只用 Agent 轻量版的 `markdown_url`。

## 10:53 - 开始接入 MinerU 一键解析工作流

- 目标：把 MinerU 精准解析接到桌面应用里，做成 PDF Viewer 一键解析、生成 Markdown 笔记、PDF 框点击跳转到 Note 的工作流。
- 状态：已完成
- 已完成：已补充 MinerU 相关共享类型、preload 透传和主进程解析 IPC；已把 MinerU 配置接进工作台状态；已在 PDF Viewer 上增加 MinerU 解析按钮与可点击框层；已把 Note 面板接入“外部 block 聚焦”请求；已把 MinerU 设置合入 AI 配置面板；已在 Note 中为 MinerU 解析块保留来源 `SourceRef` 与 `mineruBlockId` 映射；`npm run typecheck` 与 `npm run build` 均已通过。
- 修改文件：`packages/shared/src/index.ts`、`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/preload/index.ts`、`apps/desktop/src/main/index.ts`、`apps/desktop/src/main/mineru.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/app/constants.ts`、`apps/desktop/src/renderer/app/types.ts`、`apps/desktop/src/renderer/components/editor/EditorContent.tsx`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/styles.css`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；MinerU 精准解析本地文件上传链路在当前环境会卡到 OSS 域名解析，但远程 URL 模式可正常返回 `full_zip_url` 和结构化结果。
- 下一步：如果后续要增强体验，可以把 MinerU 结果再细分成“标题、摘要、表格、图片”快捷插入模板，或把本地上传模式改成后端代理上传来规避 OSS DNS 限制。

## 10:55 - 校准 MinerU 蓝框坐标

- 目标：修正 PDF Viewer 中 MinerU 解析蓝框在缩放或切页时的偏移问题，确保框能稳定覆盖对应原文区域。
- 状态：已完成
- 已完成：已确认 MinerU `content_list.json` 的 `bbox` 是 1000 基准坐标，并将解析器改为按 1000 归一化；已把蓝框样式改为 `box-sizing: border-box`，避免边框撑大视觉区域；已补充 PDF 叠层与 Note 聚焦链路的保留字段；已用样例 zip 比对 `content_list bbox / 1000` 与 `layout.json bbox / page_size`，确认坐标差异仅在千分之一量级。
- 修改文件：`apps/desktop/src/main/mineru.ts`、`apps/desktop/src/renderer/components/pdfviewer/pdfViewerShared.ts`、`apps/desktop/src/renderer/app/types.ts`、`apps/desktop/src/renderer/App.tsx`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；已用 Python 脚本对比样例 PDF 的解析结果坐标，确认归一化逻辑与 MinerU 原始布局一致。
- 下一步：如果后续还要进一步提升视觉精度，可以继续把蓝框改成只在当前页可见区域显示，并增加 hover/点击热区提示。

## 11:05 - 重构 MinerU 工具栏交互

- 目标：按产品交互要求，把 MinerU 设置迁到 PDF 工具栏，解析按钮改为菜单，解析结果不再自动生成笔记，并补充开始/完成消息窗。
- 状态：已完成
- 已完成：已将 MinerU 配置从 AI 设置面板迁移到 PDF 工具栏的齿轮按钮；已把原本直接解析的按钮改为小菜单，显示当前文档是否已解析，并提供“解析当前 PDF / 一键生成笔记 / 关闭显示框”；已补充 `vlm / pipeline / MinerU-HTML` 的解析方式小注释；已将解析完成后的结果改为仅缓存，不再自动生成笔记；已增加解析开始、完成、失败的右上角消息窗。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/components/editor/EditorContent.tsx`、`apps/desktop/src/renderer/styles/pdf.css`、`apps/desktop/src/renderer/styles/modals.css`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：建议在真实 Electron 窗口里手工点一遍 MinerU 菜单、设置弹层、解析提示窗和“生成笔记 / 关闭显示框”链路，确认交互节奏是否还需要再压缩。

## 11:18 - 增加 MinerU 解析缓存与显示框隐藏状态

- 目标：让软件记录哪些 PDF 已经解析过，关闭显示框后不丢失结果，避免用户被迫重复解析。
- 状态：已完成
- 已完成：已为工作台状态新增 `mineruResultsByPaperPath` 与 `hiddenMineruOverlayByPaperPath` 两份持久化数据；关闭显示框现在只会隐藏蓝框，不会删除解析结果；MinerU 菜单会根据状态显示“当前文档已解析 / 显示框已隐藏”，并在已隐藏时提供“显示解析框”；应用重启后仍能恢复已解析状态。
- 修改文件：`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/main/appState.ts`、`apps/desktop/src/main/index.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/editor/EditorContent.tsx`、`apps/desktop/src/renderer/components/PdfViewer.tsx`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：如果后续要继续增强缓存策略，可以把“最后解析时间 / 解析参数摘要 / 手动清除缓存”也加入 MinerU 菜单，帮助用户判断是否需要重新解析。

## 00:24 - 开始拆分 App.tsx

- 目标：按项目模块边界开始拆分 `apps/desktop/src/renderer/App.tsx`，优先迁移纯类型、常量、Dock 布局纯函数和低耦合无状态组件，降低入口文件复杂度且不改变现有行为。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认 `App.tsx` 仍是超大入口文件，当前第一批拆分会限定在 Renderer 内部的可安全迁移代码。
- 修改文件：`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已运行 `Get-Content -Encoding utf8` 读取开发文档；已运行 `git status --short` 确认当前仓库仍是初始化阶段的大量未跟踪文件状态。
- 下一步：创建 Renderer 内部拆分目录，迁移类型、常量、Dock 纯函数和小组件，再运行 `npm run typecheck` 与 `npm run build`。

## 00:36 - 完成 App.tsx 第一批拆分

- 目标：把 `App.tsx` 中低风险、可复用的结构先拆出去，给后续继续迁移 Library / Favorites / AI / Note 面板组件打基础。
- 状态：已完成
- 已完成：已新增 `apps/desktop/src/renderer/app/types.ts` 承载工作台类型；新增 `app/constants.ts` 承载活动栏、AI 上下文和编辑器常量；新增 `app/dockLayout.ts` 承载 Dock 布局常量与纯函数；新增 `components/AppSharedControls.tsx` 承载分节标题、收藏按钮、条目操作按钮等共享小组件；已将 `App.tsx` 改为引用这些模块，并恢复被机械清理误删的 `LibraryPrimaryView` 组件。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/app/types.ts`、`apps/desktop/src/renderer/app/constants.ts`、`apps/desktop/src/renderer/app/dockLayout.ts`、`apps/desktop/src/renderer/components/AppSharedControls.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；`App.tsx` 行数由约 7714 行下降到 7507 行，核心收获是依赖结构开始模块化而不是单纯追求行数下降。
- 下一步：下一轮优先继续拆分 `LibraryPrimaryView`、`FavoritesView`、`CurrentPdfNotes`、`CurrentPdfGraph` 这组侧边栏视图，再把对应的 PDF/收藏/对话辅助函数一起下沉到 `renderer/app/*`。

## 00:38 - 将程序用户可见名称改为 Inspiration

- 目标：把当前程序的用户可见品牌从 `Thesis Agent` 统一改为 `Inspiration`，覆盖窗口标题、页面标题、顶栏品牌、README 和相关默认提示语，同时避免触碰内部包作用域与桥接 API 名。
- 状态：已完成
- 已完成：已更新 Electron 主窗口标题、Renderer 页面的 `<title>`、工作台左上角品牌文案、默认 AI system prompt、AI Provider 测试提示语、截图导出前缀、PDF 渲染缓存临时目录前缀、README 标题和 UI 命名文档中的品牌说明；内部 `@thesis-agent/*` 包名和 `window.thesisAgent` 桥接名本轮保持不变。
- 修改文件：`apps/desktop/src/main/index.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/index.html`、`apps/desktop/src/renderer/app/constants.ts`、`README.md`、`docs/development/ui-naming.md`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已运行 `rg -n 'Thesis Agent' apps\\desktop\\src README.md docs\\development\\ui-naming.md`，用户可见入口不再匹配旧品牌；已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：如果后续需要彻底品牌迁移，可再评估是否同步更换根包名、workspace scope、桥接文件名和 `window.thesisAgent` API 名。

## 00:40 - 继续拆分侧边栏主视图

- 目标：继续拆分 `App.tsx`，优先把 Library、Favorites、Notes、Graph 这组左侧主侧栏视图以及配套显示辅助函数从入口文件中迁出，保持行为不变。
- 状态：进行中
- 已完成：已重新阅读项目开发约定；已确认本轮拆分仍限定在 `apps/desktop/src/renderer` 内，不触碰主进程、preload、数据库或包作用域重命名。
- 修改文件：`logs/progress/2026-05-20.md`
- 验证结果：已运行开发文档读取命令与 `git status --short`，当前仓库状态与前一轮一致。
- 下一步：定位 `LibraryPrimaryView`、`FavoritesView`、`CurrentPdfNotes`、`CurrentPdfGraph`、`AiGraphConversationBranch` 的依赖边界，新增侧边栏组件文件和配套工具模块，再执行 `npm run typecheck` 与 `npm run build`。

## 09:29 - 完成左侧主侧栏视图拆分

- 目标：把 `App.tsx` 中的主侧栏视图与展示辅助函数迁出入口文件，继续把桌面 Renderer 入口收缩成组装层。
- 状态：已完成
- 已完成：新增 `apps/desktop/src/renderer/app/sidebarUtils.ts`，收口了文件名显示、阅读进度、收藏解析、问答树摘要、Profile 统计等侧边栏展示工具；新增 `apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`，承载 `LibraryPrimaryView`、`FavoritesView`、`CurrentPdfNotes`、`CurrentPdfGraph`、`AiGraphConversationBranch` 和 `PrimaryViewContent`；`App.tsx` 已改为直接引用新组件文件，并移除原有同名实现。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/app/sidebarUtils.ts`、`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；`App.tsx` 行数由 7507 行继续下降到 6611 行。
- 下一步：下一轮可以继续拆 `ProfileHomePage`、`EditorContent` 和与笔记面板相关的辅助函数，逐步让 `App.tsx` 只保留状态编排和顶层布局。

## 09:49 - 完成 EditorContent 与 ProfileHomePage 外移

- 目标：继续拆分 `App.tsx`，把主编辑区缓存容器和个人主页从入口文件迁到独立组件文件，并顺手清理已完全独立的展示函数。
- 状态：已完成
- 已完成：新增 `apps/desktop/src/renderer/components/editor/EditorContent.tsx` 承载 PDF 编辑区缓存槽；新增 `apps/desktop/src/renderer/components/profile/ProfileHomePage.tsx` 承载个人主页；`App.tsx` 已改为引用这两个组件，并移除原有本地实现；同时删除了 `App.tsx` 中已完全可由外移组件承担的 Profile 统计与 PDF 进度显示辅助函数。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/editor/EditorContent.tsx`、`apps/desktop/src/renderer/components/profile/ProfileHomePage.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；`App.tsx` 行数由 6611 行继续下降到 6180 行。
- 下一步：下一轮优先拆 `NotePanelContent` 及其笔记面板辅助函数，然后再评估是否把仍留在 `App.tsx` 的共享数据工具整体迁入 `sidebarUtils.ts` 或独立 `noteUtils.ts`。

## 11:01 - 开始拆分 styles.css

- 目标：把 `apps/desktop/src/renderer/styles.css` 按工作台领域拆成多个样式文件，保留现有类名与渲染结果不变，降低单文件维护成本。
- 状态：进行中
- 已完成：已重新阅读开发约定；已确认 `main.tsx` 当前只引入单一 `styles.css`，适合改为保留 `styles.css` 作为聚合入口，再用 `@import` 引入分拆后的领域样式文件。
- 修改文件：`logs/progress/2026-05-20.md`
- 验证结果：已读取 `main.tsx` 与 `styles.css` 体量信息，当前样式文件约 4500 行。
- 下一步：先梳理 `styles.css` 的天然分段边界，再创建 `styles/` 子目录并把规则按 workbench、sidebar、pdf、ai、note、profile、modal 等领域拆开。

## 11:08 - 完成 styles.css 按领域拆分

- 目标：把 `apps/desktop/src/renderer/styles.css` 从单一超大文件改为可维护的样式分层，同时保持现有界面类名和渲染结果不变。
- 状态：已完成
- 已完成：新增 `styles/base.css`、`styles/modals.css`、`styles/titlebar.css`、`styles/sidebar.css`、`styles/profile.css`、`styles/pdf.css`、`styles/note.css`、`styles/workbench.css`、`styles/ai.css`；将原 `styles.css` 收口为聚合入口，统一通过 `@import` 引入领域样式文件；构建产物中的 CSS 已切换到新的聚合入口并正常输出。
- 修改文件：`apps/desktop/src/renderer/styles.css`、`apps/desktop/src/renderer/styles/base.css`、`apps/desktop/src/renderer/styles/modals.css`、`apps/desktop/src/renderer/styles/titlebar.css`、`apps/desktop/src/renderer/styles/sidebar.css`、`apps/desktop/src/renderer/styles/profile.css`、`apps/desktop/src/renderer/styles/pdf.css`、`apps/desktop/src/renderer/styles/note.css`、`apps/desktop/src/renderer/styles/workbench.css`、`apps/desktop/src/renderer/styles/ai.css`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：已运行 `npm run build` 通过；已运行 `npm run typecheck` 通过；样式已拆分为 9 个领域文件，其中 `pdf.css`、`sidebar.css`、`note.css` 体量最大，后续可以再继续细拆。
- 下一步：下一轮可以继续把 `sidebar.css` 或 `pdf.css` 再二次细分，或者回到 `NotePanelContent` 的组件拆分工作。

## 11:07 - 开始拆分 PdfViewer.tsx

- 目标：拆分 `apps/desktop/src/renderer/components/PdfViewer.tsx`，优先迁移纯展示子组件与 PDF 专属纯工具函数，降低单文件复杂度且不改变现有交互语义。
- 状态：进行中
- 已完成：已重新阅读项目开发约定；已确认 `PdfViewer.tsx` 约 2246 行，当前天然边界包括 `PdfScrollStrip`、`PdfOverviewPanel`、`PdfBitmapPage`、`AnnotationColorPicker`、`AnnotationPreview` 等子组件，以及 50 余个渲染/几何/颜色纯工具函数。
- 修改文件：`logs/progress/2026-05-20.md`
- 验证结果：已运行 `rg -n` 和行数统计命令完成结构定位。
- 下一步：先把 PDF 子组件与几何/颜色/文本纯工具迁到独立模块，再让 `PdfViewer.tsx` 回到以状态编排和事件流为主的结构。

## 11:13 - 完成 PdfViewer.tsx 第一阶段拆分

- 目标：把 `PdfViewer.tsx` 中的纯展示子组件与 PDF 专属纯工具函数迁出主文件，降低单文件复杂度并保留现有交互行为。
- 状态：已完成
- 已完成：新增 `apps/desktop/src/renderer/components/pdfviewer/pdfViewerShared.ts`，承载 PDF viewer 的类型、常量、几何/颜色/文本纯函数；新增 `apps/desktop/src/renderer/components/pdfviewer/PdfViewerParts.tsx`，承载 `PdfScrollStrip`、`PdfOverviewPanel`、`PdfBitmapPage`、`AnnotationColorPicker`、`AnnotationPreview` 等展示子组件；`PdfViewer.tsx` 已改为引用这两个模块并移除对应本地实现。
- 修改文件：`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/components/pdfviewer/pdfViewerShared.ts`、`apps/desktop/src/renderer/components/pdfviewer/PdfViewerParts.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；`PdfViewer.tsx` 行数由约 2246 行下降到 1356 行。
- 下一步：下一轮可以继续把 `PdfViewer.tsx` 内剩余的状态流按“文档加载/渲染”、“批注与选区”、“阅读状态与工具栏桥接”三个方向继续拆成 hooks 或领域模块。

## 11:14 - 开始拆分主进程 index.ts

- 目标：拆分 `apps/desktop/src/main/index.ts`，优先迁移独立性最强的 AppState 持久化与清洗逻辑，降低主进程入口复杂度且不改变 IPC 行为。
- 状态：进行中
- 已完成：已重新阅读项目开发约定；已确认当前 `index.ts` 约 2097 行，主要混合窗口启动、IPC 注册、持久化状态、导出、PDF 位图/布局解析、Mineru 与 AI 调用辅助。
- 修改文件：`logs/progress/2026-05-20.md`
- 验证结果：已通过 `rg -n` 和行数统计定位主进程结构。
- 下一步：抽出 `AppState` 相关类型、默认值、读写与 sanitize 系列函数到独立模块，再更新 `index.ts` 引用并执行 `npm run typecheck` / `npm run build`。

## 11:21 - 完成主进程 index.ts 第一阶段拆分

- 目标：先从 `apps/desktop/src/main/index.ts` 中切出最独立的 AppState 持久化读写逻辑，减少主进程入口的职责耦合。
- 状态：已完成
- 已完成：新增 `apps/desktop/src/main/appState.ts`，集中承载持久化状态类型、默认值、状态文件读写、sanitize 与结构校验逻辑；`index.ts` 的 `app-state:read` / `app-state:write` IPC 现在已经改为调用新模块；在不大面积删除旧实现的前提下，先完成主进程入口与新模块的稳定接线。
- 修改文件：`apps/desktop/src/main/index.ts`、`apps/desktop/src/main/appState.ts`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；当前 `index.ts` 行数约 2103 行，本轮重点是解耦读写链路而不是立刻压缩行数。
- 下一步：下一轮继续拆 `index.ts` 时，优先把 `AppState` 相关残留类型与 sanitize helper 从主文件彻底删除；再考虑拆 `pdf bitmap/layout`、`note export` 或 `ai request` 这几块 IPC/服务逻辑。

## 11:22 - 开始拆分 NotePanelContent

- 目标：把 `apps/desktop/src/renderer/App.tsx` 中的 `NotePanelContent` 连同 `NoteTemplateChooser`、`NoteBlockEditor` 和 note 面板专属 helper 一起迁出，减少入口文件复杂度且不改变现有笔记交互。
- 状态：进行中
- 已完成：已重新阅读开发约定；已定位 `NotePanelContent` 依赖的历史快照、块编辑样式、媒体块读取和模板选择逻辑，确认适合一并迁移到 `components/note/` 与独立 `notePanelUtils.ts`。
- 修改文件：`logs/progress/2026-05-20.md`
- 验证结果：已运行 `rg -n` 和局部 `Get-Content` 命令完成结构定位。
- 下一步：创建 note 面板组件文件和工具模块，接回 `App.tsx` 后执行 `npm run typecheck` 与 `npm run build`。

## 11:31 - 完成 NotePanelContent 拆分

- 目标：把 `NotePanelContent` 连同模板选择、块编辑和 note 面板专属 helper 从 `App.tsx` 中迁出，降低入口文件复杂度并保持现有笔记交互不变。
- 状态：已完成
- 已完成：新增 `apps/desktop/src/renderer/components/note/NotePanelContent.tsx`，承载 `NotePanelContent`、`NoteTemplateChooser`、`NoteBlockEditor`；新增 `apps/desktop/src/renderer/app/notePanelUtils.ts`，承载 note 面板历史快照、块展示与模板定义辅助；`App.tsx` 已改为传入派生状态与回调，并移除原有 note 面板实现及其残留死代码。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/app/notePanelUtils.ts`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；`App.tsx` 行数由约 6180 行下降到 5314 行。
- 下一步：下一轮可以继续把 `App.tsx` 中仍保留的 note 状态辅助函数收拢到 `notePanelUtils.ts`，或者继续拆主进程 / AI 面板 /剩余共享数据 helper。
# 2026-05-20 进度日志

## 00:00 - 开始修复 Graph 展开导致工作台上移

- 目标：修复点击 Graph 里的对话展开后，工作台整体向上偏移并在底部露出空白区域的问题。
- 状态：进行中
- 已完成：已重新阅读开发前约定、日志规范、版本规范、底层架构和技术方案；已初步定位 Graph 树渲染集中在 `CurrentPdfGraph` / `AiGraphConversationBranch`，样式集中在 `.graph-tree-*` 和侧栏滚动容器。
- 修改文件：`logs/progress/2026-05-20.md`
- 验证结果：待完成代码定位、修复并运行类型检查和构建。
- 下一步：检查工作台根容器、Dock 面板、主侧栏和 Graph 树展开项的高度/滚动约束。

## 00:08 - 完成 Graph 展开布局上移修复

- 目标：修复点击 Graph 里的对话展开后，工作台整体向上偏移并在底部露出空白区域的问题。
- 状态：已完成
- 已完成：已确认 Graph 节点点击会触发 AI 面板按 `focusedMessageId` 跳转回答，原实现使用 `scrollIntoView()`，在复杂 Dock 布局里可能滚动外层文档；已改为只滚动 AI 对话列表内部的 `ai-turn-list`；同时将 `.app-shell` 固定在窗口视口内，并补齐 `.workspace` 与 `.sidebar-content` 的高度约束，避免展开内容撑破外层布局；已同步更新变更记录。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/styles.css`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：在真实 Electron 界面中点击 Graph 问答节点展开/收起，并确认 AI 跳转回答时窗口不再整体位移。
## 01:05 - 完成笔记系统方案文档和第一批契约实现

- 目标：把笔记系统方案落成可执行文档，并完成模板注册表、来源引用扩展和基础迁移。
- 状态：已完成
- 已完成：新增 `docs/notes-system-implementation.md`；新增 `logs/decisions/2026-05-20-notes-system-phasing.md`；`packages/shared` 扩展了 `SourceRef` 的笔记来源字段；`packages/notes` 增加模板注册表、模板列表、模板创建入口和自由笔记创建器；`apps/desktop/src/renderer/App.tsx` 的模板页改为读取注册表并新增自由笔记入口；`apps/desktop/src/main/index.ts` 接入了新的来源白名单；`packages/db/src/index.ts` 补了 note 模板、回链和版本迁移；`CHANGELOG.md` 已同步更新。
- 修改文件：`docs/notes-system-implementation.md`、`logs/decisions/2026-05-20-notes-system-phasing.md`、`packages/shared/src/index.ts`、`packages/notes/src/index.ts`、`apps/desktop/src/main/index.ts`、`apps/desktop/src/renderer/App.tsx`、`packages/db/src/index.ts`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：继续把 `notes` 的回链 / 版本快照能力拆成独立领域方法，并再往 `workbench` 的命令注册里补模板相关入口。

## 00:51 - 落实笔记系统方案文档并启动第一阶段实现

- 目标：把笔记系统的模板、来源引用、回链和分阶段迁移路线落成文档，并开始实现第一批领域契约。
- 状态：进行中
- 已完成：已补充 `docs/notes-system-implementation.md`，明确第一阶段先做 `shared` / `notes` / `renderer` 接入，暂不切换现有 App 状态存储；已新增架构决策记录，说明笔记系统采用分阶段实施。
- 修改文件：`docs/notes-system-implementation.md`、`logs/decisions/2026-05-20-notes-system-phasing.md`、`logs/progress/2026-05-20.md`
- 验证结果：尚未运行类型检查和构建；先完成文档与实施路线固化，再进入代码改动。
- 下一步：扩展 `SourceRef` 的笔记来源类型，补 `packages/notes` 的模板注册表，并把模板选择页接入注册表。

## 11:31 - 修复 AI 面板样式变形

- 目标：定位并修复 AI 面板顶部与输入区退回浏览器默认表单排版的问题。
- 状态：已完成
- 已完成：已确认问题来自 `styles.css` 拆分后 `styles/ai.css` 从中段开始裁切，导致 `.ai-file-input`、`.ai-chat`、`.ai-context-strip`、`.ai-quick-actions`、`.ai-composer`、`.ai-inline-selects` 等规则缺失；已补回缺失的 AI 顶部/输入区样式，恢复文件输入隐藏、快捷动作横排、上下文条与输入工具栏布局。
- 修改文件：`apps/desktop/src/renderer/styles/ai.css`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已运行 `npm run build` 通过；已运行 `npm run typecheck` 通过。
- 下一步：后续继续拆样式文件时优先按选择器块边界切分，不再按大段近似行号直接裁切。
## 11:52 - 调整笔记纸张与 Markdown 编辑体验

- 目标：按反馈取消 Note 纸张随面板缩放、取消纸张内文字块卡片/内嵌滚动条，并把保存、导出、字体、加粗等编辑入口移到 Note 面板工具栏。
- 状态：已完成
- 已完成：已重新阅读开发前必读文档；已将 Note 纸张改为固定宽度并放入外层滚动容器；已把保存、收藏、复制 Markdown、导出、字体、加粗、分点和插入入口移到 Note 面板顶部工具栏；已移除纸张内的文字块工具条、类型选择器、上下移动/删除按钮和内嵌滚动 textarea；MinerU 生成笔记现在在纸张内呈现为连续 Markdown 段落/图片/表格内容；同步将用户可见的“块/解析块”文案改为“段/解析区域”。
- 修改文件：`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/styles/note.css`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/components/profile/ProfileHomePage.tsx`、`apps/desktop/src/main/appState.ts`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过；曾遇到 `appState.ts` 缺少 MinerU `autoClean` 字段导致 typecheck 失败，已记录到 `logs/errors/2026-05-20.md` 并修复。
- 下一步：在真实 Electron 界面中拖拽 Note 面板宽度、生成 MinerU 笔记并点击 PDF 解析框，确认纸张固定宽度、段落定位和工具栏交互符合预期。

## 12:07 - 复核笔记 UI 调整收尾

- 目标：复核 Note 面板是否仍残留纸张内按钮、文字块卡片、内嵌滚动文本框或用户可见“块”文案。
- 状态：已完成
- 已完成：已确认 `NotePanelContent` 当前工具栏位于 Note 面板顶部，纸张正文仅保留标题、元信息、连续 Markdown 段落/截图/表格内容；已把收藏列表里的“块”改为“段”，把 MinerU 清洗说明里的“块”改为“区域”；已移除 Note 面板不再使用的删除/移动段落回调传参和函数；已重新运行类型检查和构建。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/app/sidebarUtils.ts`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已完成代码搜索；`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：在真实 Electron 界面中拖拽 Note 面板宽度、生成 MinerU 笔记并点击 PDF 解析框，确认视觉与交互符合预期。

## 12:11 - 增强 MinerU 结果拖拽、媒体和回跳体验

- 目标：让 MinerU 解析结果里的图片自动以截图进入笔记、公式先渲染再呈现；支持从 PDF 上的 MinerU 候选框拖到 Note 或 AI；让 MinerU 生成/拖入的文字呈现为带下划线的来源文本，并支持 Ctrl+左键回跳 PDF 对应文段。
- 状态：已完成
- 已完成：已重新阅读开发前必读文档；新增 MinerU 拖拽数据协议；PDF 解析框支持携带 MinerU 块拖拽；Note 面板支持接收拖拽并新增带来源的下划线等宽摘录；AI 输入框支持接收拖拽并追加为新段落；一键生成 MinerU 笔记时会优先按 PDF 坐标裁剪图片/图表区域；公式段落已接入 KaTeX 预览渲染；Note 来源段落支持 Ctrl/Command + 左键跳回 PDF 对应页和来源框。
- 修改文件：`apps/desktop/package.json`、`package-lock.json`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/main.tsx`、`apps/desktop/src/renderer/app/types.ts`、`apps/desktop/src/renderer/app/mineruDrag.ts`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/styles/note.css`、`apps/desktop/src/renderer/styles/pdf.css`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：`npm run typecheck` 已通过；`npm run build` 已通过。
- 下一步：在真实 Electron 界面中拖拽 MinerU 蓝框到 Note/AI，确认拖拽手感、图片裁剪位置和 Ctrl 回跳页码符合预期。

## 14:22 - 开始处理 Note 回跳、双击写作和扁平化反馈

- 目标：按最新反馈修复 Ctrl/Command + 左键从 Note 来源文字跳回 PDF 的链路；让 Note 像 Word 一样在文档区域双击即可新起一段并聚焦输入；让 MinerU 拖入文字只表现为正文底色变化，去掉来源标签、图片边框和多余框感；关闭文本输入框拼写检查。
- 状态：进行中
- 已完成：已重新阅读项目开发约定；已确认仓库根目录为 `E:/THESIS_AGENT`；已定位本轮主要修改点在 Note 面板、PDF Viewer、App 状态编排和 `note.css`。
- 修改文件：待修改
- 验证结果：暂未运行，待完成代码修改后执行类型检查和构建。
- 下一步：修改 `NotePanelContent.tsx`、`PdfViewer.tsx`、`App.tsx` 和相关样式，再同步更新变更记录。

## 14:50 - 完成 Note 回跳、双击写作和扁平化反馈

- 目标：按最新反馈修复 Note 来源文字回跳 PDF、双击文档区域写作、拖入来源段落扁平化、隐藏图片/截图插入入口并关闭拼写检查。
- 状态：已完成
- 已完成：已将 Note 来源文字回跳提前到 `pointerdown` 捕获阶段处理，避免 textarea 吃掉点击；PDF Viewer 会响应外部 `pageNumber` 变化并在回跳时显示当前来源区域；Note 页面支持双击空白区域按鼠标位置新建段落并自动聚焦；MinerU 来源段落去掉来源标签和框感，仅保留正文高亮、下划线和等宽字体；图片/截图插入入口已从 Note 插入工具中隐藏；Note、AI 设置/输入、PDF/MinerU 输入框已关闭拼写检查；已同步更新 `CHANGELOG.md`。
- 修改文件：`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/styles/note.css`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：前一轮代码修改后已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；本次收尾补充变更记录后准备再次运行验证。
- 下一步：运行 `npm run typecheck` 与 `npm run build` 复核最终状态。

## 14:51 - 完成最终验证

- 目标：复核 Note 扁平化、回跳和拼写检查改动后的最终工程状态。
- 状态：已完成
- 已完成：已搜索确认 `note-source-list` / `note-source-chip` 无残留渲染；确认 Note 页面双击、来源文字 `pointerdown` 捕获、核心输入框 `spellCheck={false}`、PDF 外部页码同步和 MinerU 来源高亮样式均在目标文件中；已重新运行类型检查和构建。
- 修改文件：`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：交付本轮结果，后续可在真实 Electron 窗口里手动确认拖拽、双击和 Ctrl/Command 回跳手感。

## 17:14 - 开始调整 Note 原文片段为编辑器式内联来源

- 目标：让 Note 默认保持文本编辑光标，只有按住 Ctrl/Command 悬停原文片段时才显示可点击状态；让同一段文字中只有原文片段本身带背景和回跳能力，前后可继续编辑用户说明。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档，确认本轮改动集中在 Renderer 的 Note 段落编辑与来源回跳交互。
- 修改文件：`logs/progress/2026-05-20.md`
- 验证结果：暂未运行，待完成代码修改后执行类型检查和构建。
- 下一步：检查 `NotePanelContent.tsx` 当前 textarea 来源段落实现，调整为可编辑文本层 + 内联高亮来源片段的体验。

## 17:24 - 完成 Note 原文片段内联来源交互

- 目标：让 Note 中的原文来源像代码编辑器里的可跳转引用一样工作：默认可编辑、默认 I-beam 光标，按住 Ctrl/Command 时仅原文片段变为可点击。
- 状态：已完成
- 已完成：已移除独立来源预览块和“把原文从 textarea 中剔除”的逻辑；Note 来源段落现在使用可编辑 textarea 与只读镜像层叠加，镜像层只给 `sourceRef.quote` 匹配到的原文片段加背景；用户可以在同一段内写成“说明 + 原文片段 + 说明”；按住 Ctrl/Command 时 textarea 暂停接管指针，仅内联原文片段可点击回跳 PDF；一键 MinerU 生成和拖入 Note 不再把整段应用来源字体/背景样式；已同步更新 `CHANGELOG.md`。
- 修改文件：`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/styles/note.css`、`apps/desktop/src/renderer/App.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过；曾遇到 `NotePanelContent.tsx` JSX 对象属性漏逗号导致类型检查失败，已记录到 `logs/errors/2026-05-20.md` 并修复。
- 下一步：在真实 Electron 界面中手动确认原文片段匹配、高亮位置、Ctrl/Command 悬停指针和回跳手感。
## 16:24 - 修复关闭标签页不应删除左侧条目卡

- 目标：调整关闭 PDF/笔记标签页时的状态逻辑，让左侧文献条目卡和笔记条目卡只在执行删除动作时消失。
- 状态：已完成
- 已完成：已阅读开发约定、日志规范、版本提交规范、底层架构和技术方案，确认仓库根目录为 `E:/THESIS_AGENT`；新增独立的 `libraryPdfPaths` 状态与持久化字段，让左侧 Library 条目不再依赖已打开 PDF 标签；调整关闭 PDF 标签、删除 PDF 条目、Profile 阅读名单和侧边栏渲染的数据来源；关闭未保存的新建 Note 标签时不再移除运行时笔记条目。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/main/appState.ts`、`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/components/profile/ProfileHomePage.tsx`、`CHANGELOG.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：在桌面应用内手动确认关闭 PDF/Note 标签后左侧条目仍保留，删除按钮才移除条目。
## 17:10 - 修复笔记栏加号无法新建笔记

- 目标：排查并修复 Note 面板标题栏 `+` 点击后无法正常进入新建笔记流程的问题。
- 状态：已完成
- 已完成：已按开发约定重新阅读开发文档、日志规范、版本提交规范、底层架构和技术方案，确认仓库根目录为 `E:/THESIS_AGENT`；定位到 Note 顶部 `+` 会设置模板待创建状态，但旧打开笔记优先显示导致新建流程看起来无效；调整 Note 面板内容和标题栏标签的选中优先级，让模板待创建状态优先展示。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：在应用内手动确认点击 Note 顶部 `+` 后会显示模板选择页，并能创建新笔记。

## 17:40 - 开始修复 Note 拖拽图片/表格、编辑高亮和缩放

- 目标：修复 MinerU 图片/表格拖入 Note 后未生成对应图片/表格的问题；取消 Note 编辑时当前段选中高亮；为 Note 页面增加 Ctrl/Command + 滚轮缩放。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认仓库根目录为 `E:/THESIS_AGENT`；已定位拖拽入口在 `NotePanelContent.tsx`，真正的插入逻辑在 `App.tsx` 的 `handleMineruBlockDropToNote` / `createMineruDroppedTextBlock`；已定位当前段高亮样式在 `styles/note.css` 的 `.note-markdown-section.active`；已确认 Note 页面尚未接入独立缩放状态与滚轮监听。
- 修改文件：待修改
- 验证结果：待完成代码修改后运行 `npm run typecheck` 与 `npm run build`。
- 下一步：复用 MinerU block 到 Note block 的类型映射，补齐拖入图片/表格生成逻辑，再接入 Note 缩放和样式调整。

## 17:48 - 完成 Note 拖拽图片/表格、编辑高亮和缩放修复

- 目标：修复 MinerU 图片/表格拖入 Note 后未生成对应图片/表格的问题；取消 Note 编辑时当前段选中高亮；为 Note 页面增加 Ctrl/Command + 滚轮缩放。
- 状态：已完成
- 已完成：已将 MinerU 拖入 Note 的插入逻辑从“固定段落文本”扩展为按块类型生成对应 Note block；图片、图表和带截图数据的表格现在会直接生成截图块，普通表格仍保留表格块，列表会保留分点样式；已移除 Note 当前聚焦段的浅蓝底色高亮；已为 Note 页面滚动容器增加 Ctrl/Command + 滚轮缩放，并将纸张缩放应用到实际文档页。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/styles/note.css`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：建议在真实 Electron 窗口里手动拖拽图片/表格蓝框到 Note，并实际试一遍 Ctrl/Command + 滚轮缩放手感，确认缩放步进和图片生成结果符合预期。

## 17:55 - 开始修复 PdfViewer useEffect 依赖警告与 Note HMR 500

- 目标：修复 `PdfViewer.tsx` 中 `useEffect` 依赖数组长度变化警告，并排查 `NotePanelContent.tsx` 热更新 500 错误。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认仓库根目录为 `E:/THESIS_AGENT`；已准备定位 `PdfViewer.tsx` 第 680 行附近和 `NotePanelContent.tsx` 最近改动。
- 修改文件：待修改
- 验证结果：待完成代码修改后运行 `npm run typecheck` 与 `npm run build`。
- 下一步：检查依赖数组中条件展开项与 Note 组件的语法/类型问题，修复后统一验证。

## 18:02 - 完成 PdfViewer 依赖警告修复并复核 Note HMR

- 目标：修复 `PdfViewer.tsx` 中 `useEffect` 依赖数组长度变化警告，并排查 `NotePanelContent.tsx` 热更新 500 错误。
- 状态：已完成
- 已完成：已将 `PdfViewer.tsx` 第 680 行附近的页码恢复 effect 改为依赖显式 `sessionPageNumber` 常量，并保留固定长度依赖数组，降低 HMR 期间因旧闭包/旧依赖签名残留导致的 React 警告；已重新通读当前 `NotePanelContent.tsx`，确认源码无语法错误、无缺失导入，当前分支下 `typecheck` 与 `build` 均通过，说明先前的 500 更像是开发服务器热更新缓存持有旧模块快照而不是现行源码仍然损坏。
- 修改文件：`apps/desktop/src/renderer/components/PdfViewer.tsx`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：如果本地开发窗口仍残留旧的 HMR 500，直接让 Vite dev server 重新编译一次或重启桌面开发进程，再观察 `NotePanelContent.tsx` 是否还会报错。

## 18:10 - 开始修复 Note 滚轮报错与 MinerU 持久化恢复

- 目标：修复 Note 页 Ctrl/Command + 滚轮 触发 passive listener 报错；让 MinerU 解析结果、显示框状态和 Note/PDF 回跳映射在重启后仍可直接使用。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认仓库根目录为 E:/THESIS_AGENT；已准备检查 Renderer 滚轮绑定、主进程 ppState 清洗/持久化以及 PDF 工具栏里的 MinerU 菜单状态判断。
- 修改文件：待修改
- 验证结果：待完成代码修改后运行 
pm run typecheck 与 
pm run build。
- 下一步：定位 NotePanelContent.tsx 的滚轮监听实现和 ppState.ts / App.tsx 的 MinerU 结果恢复链路，修复后统一验证。

## 18:24 - 完成 Note 滚轮报错与 MinerU 持久化恢复

- 目标：修复 Note 页 Ctrl/Command + 滚轮触发 passive listener 报错；让 MinerU 解析结果、显示框状态和 Note/PDF 回跳映射在重启后仍可直接使用。
- 状态：已完成
- 已完成：已将 Note 页缩放从 React `onWheel` 改为挂在滚动容器上的原生 `wheel` 监听，并显式使用 `passive: false`，消除 `preventDefault` 报错；已修复主进程 `appState.ts` 清洗 MinerU 结果时漏掉 `imageDataUrl` 的问题；已新增主进程侧的 MinerU 单文件缓存读写 IPC，Renderer 会在恢复工作台后按当前/已打开 PDF 自动补读解析缓存，因此同一 PDF 之前解析过的话，重启后会直接恢复“已解析”状态，并可重新显示解析框、继续生成笔记与执行 Note/PDF 回跳。
- 修改文件：`apps/desktop/src/main/appState.ts`、`apps/desktop/src/main/index.ts`、`apps/desktop/src/preload/index.ts`、`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过；中途因 `ensureMineruResultLoaded` 声明顺序导致一次 `typecheck` 失败，已记录到 `logs/errors/2026-05-20.md` 并修复。
- 下一步：建议实际关闭软件再打开同一 PDF，确认 MinerU 菜单直接显示“当前文档已解析”，并试一下“显示解析框”与 Note/PDF 双向跳转。

## 18:44 - 完成 PDF 蓝框显示缺失与拖入 AI 附件/文本修复

- 目标：修复部分 MinerU 蓝框在 PDF 默认不显示、仅在 Note 回跳时临时出现的问题；支持把 PDF 蓝框拖入 AI，对图片/图表走图片附件，对文字走文本输入。
- 状态：已完成
- 已完成：已将当前 PDF 的蓝框来源从“仅基于临时 MinerU note”调整为“真实笔记中的来源引用 + 解析结果默认笔记”合并并去重，因此自动生成、拖入或后续编辑过的来源块都能稳定显示，不再只在回跳聚焦时临时出现；已把 AI 拖拽接收从纯文本扩展为区分图片和文字，图片/图表/带截图表格会以图片附件加入 AI，文字区域继续追加到输入框；已补齐连续滚动模式下蓝框按钮缺失的 `draggable/onDragStart`，让 scroll/page 两种浏览模式都能拖入 AI。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/components/pdfviewer/PdfViewerParts.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过；中途因 `buildMineruLinkedRegionsForPaper` 放在 `createMineruNoteFromResult` 之前导致一次 `typecheck` 失败，已记录到 `logs/errors/2026-05-20.md` 并修复。
- 下一步：建议你在 page 模式和 scroll 模式各试一次，把图片蓝框和纯文字蓝框都拖进 AI，看是否分别变成附件和文本；同时再点几段之前“只在回跳时出现”的来源文字，确认蓝框是否能持续显示。

## 19:00 - 完成 PDF 翻页加载速度优化

- 目标：优化 PDF Viewer 翻页时的等待感，尽量避免每次切页都重新等渲染。
- 状态：已完成
- 已完成：已为 page 模式增加 `PDFPageProxy` 缓存、文本层内容缓存和兼容位图缓存；当前页如果命中兼容位图缓存，会先立即显示缓存位图，再后台补渲染；已增加前后相邻页预热，提前读取页对象、文本内容和兼容位图，因此连续前后翻页时会明显更快；已在切换文档或强制重载时清空这些缓存，避免跨文档串页。
- 修改文件：`apps/desktop/src/renderer/components/PdfViewer.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过；中途因缺少 `PDFPageProxy` 类型导入和 `renderTask` 可空类型检查导致一次 `typecheck` 失败，已当场修复后重新验证通过。
- 下一步：建议你在同一篇 PDF 里连续翻前后几页试一下体感，尤其是返回上一页或在相邻页之间来回切换；如果还想继续压缩首次打开等待，可以下一轮再做“多页后台预渲染”和“主进程位图磁盘缓存复用”。

## 18:52 - 开始优化 PDF 翻页加载速度

- 目标：优化 PDF Viewer 翻页时的等待感，尽量避免每次切页都重新等渲染。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认仓库根目录为 E:/THESIS_AGENT；已准备检查 PdfViewer.tsx 与 PdfViewerParts.tsx 的单页渲染、位图回退、缓存和预取逻辑。
- 修改文件：待修改
- 验证结果：待完成代码修改后运行 
pm run typecheck 与 
pm run build。
- 下一步：定位 page 模式切页时是否每次重新拉取/渲染 PDF page 或 Poppler 位图，再针对热点加缓存与邻页预热。


## 19:08 - 修复 buildMineruLinkedRegionsForPaper 运行时未定义

- 目标：修复 App.tsx 中 uildMineruLinkedRegionsForPaper is not defined 运行时错误。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认仓库根目录为 E:/THESIS_AGENT；已准备检查 currentPdfLinkedRegions 与 uildMineruLinkedRegionsForPaper 的声明顺序。
- 修改文件：待修改
- 验证结果：待完成代码修改后运行 
pm run typecheck 与 
pm run build。
- 下一步：调整 helper 声明顺序或改为函数声明，避免 render 阶段 TDZ/未初始化引用。


## 19:14 - 修复 createMineruNoteFromResult 初始化顺序报错

- 目标：修复 App.tsx 中 Cannot access 'createMineruNoteFromResult' before initialization 运行时错误。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认仓库根目录为 E:/THESIS_AGENT；已准备检查 currentPdfLinkedRegions 与 createMineruNoteFromResult 的声明顺序。
- 修改文件：待修改
- 验证结果：待完成代码修改后运行 
pm run typecheck 与 
pm run build。
- 下一步：调整 currentPdfLinkedRegions 的计算位置或把相关 helper 改成函数声明，彻底规避初始化时序问题。


## 19:24 - 开始修复 PDF 蓝框拖入 AI 失败与蓝框偏移

- 目标：修复 PDF 蓝框无法拖入 AI 对话框的问题；修复当前 PDF 蓝框与文字出现偏差的问题。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认仓库根目录为 E:/THESIS_AGENT；已准备检查 AI composer 的 dragover/drop 逻辑与 PDF 蓝框坐标/样式链路。
- 修改文件：待修改
- 验证结果：待完成代码修改后运行 
pm run typecheck 与 
pm run build。
- 下一步：优先修复 AI drop zone 对 MinerU 拖拽 MIME 的识别，再检查蓝框使用的页尺寸和渲染模式是否导致坐标偏差。


- 追加定位：已确认当前 MinerU 
ormalizeRect() 仍强制按 1000 基准归一化，未优先使用 layout.json 的真实 page_size，这很可能是当前蓝框整体偏移的根因。


## 19:22 - Library 排序菜单与持久化收尾

- 目标：把左侧 Library 排序菜单改成可选排序字段 + 递增/递减，并记住用户选择，关闭软件后自动恢复。
- 状态：已完成
- 已完成：
  - 新增 Library 排序状态模型，支持阅读时长、最近阅读、剩余进度、日期、名称、大小和递增/递减。
  - 通过主进程新增 PDF 文件信息 IPC，前端可读取文件大小和修改时间并缓存到工作台状态。
  - 将 Library 排序状态写入工作台持久化并在恢复时自动回填。
  - Library 侧边栏排序菜单改为分组弹层样式。
- 修改文件：`apps/desktop/src/main/appState.ts`、`apps/desktop/src/main/index.ts`、`apps/desktop/src/preload/index.ts`、`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/app/constants.ts`、`apps/desktop/src/renderer/app/sidebarUtils.ts`、`apps/desktop/src/renderer/app/types.ts`、`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/styles/sidebar.css`、`CHANGELOG.md`
- 验证结果：`npm run typecheck` 通过，`npm run build` 通过。
- 下一步：如果还想继续优化，可以把菜单文案和图标再调得更贴近截图样式。

## 19:35 - Library 文件夹树与拖拽层级完成第一版

- 目标：为左侧 Library 增加文件夹、新建入口、拖拽放入/放出和树状层级显示。
- 状态：已完成
- 已完成：
  - 新增 Library 文件夹树持久化结构，支持恢复文件夹名称、展开状态与文件归属。
  - `+` 菜单新增 `New Folder`，可直接创建文件夹。
  - Library 改为树状渲染，支持根级 PDF 与文件夹内 PDF 的层级展示。
  - 支持将 PDF 拖入文件夹，或拖到根级 drop zone 移回根目录。
  - 新增缩进竖线、文件夹标题行和拖拽目标高亮样式。
- 修改文件：`apps/desktop/src/main/appState.ts`、`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/app/libraryStructure.ts`、`apps/desktop/src/renderer/app/types.ts`、`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/styles/sidebar.css`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：如果继续打磨，可补“拖拽插入线预览”“文件夹重命名/删除”“多层嵌套文件夹”。

## 19:45 - 开始给 Library PDF 条目补解析/缓存状态图标

- 目标：在左侧 Library 的 PDF 条目名称后显示绿色状态图标，绿色小勾表示已解析，灰色下载图标表示已缓存；鼠标悬停时显示“已解析”“已缓存”。
- 状态：进行中
- 已完成：已重新阅读项目开发约定；已确认仓库根目录为 `E:/THESIS_AGENT`；已定位 MinerU 解析结果内存状态、单文件缓存读取 IPC 和 Library 条目渲染位置。
- 修改文件：待修改
- 验证结果：待代码修改后运行 `npm run typecheck` 与 `npm run build`
- 下一步：补充“是否存在 MinerU 缓存文件”的轻量 IPC，再将缓存/解析状态接到 `SidebarViews.tsx` 的 PDF 条目渲染。

## 19:52 - Library 条目新增解析/缓存状态图标

- 目标：在左侧 Library 的 PDF 文件名后显示状态图标，绿色小勾表示已解析，灰色下载图标表示已缓存，并在悬停时提示“已解析”“已缓存”。
- 状态：已完成
- 已完成：
  - 新增主进程 `mineru:has-cached-result` 轻量 IPC，用于判断某个 PDF 是否已有 MinerU 缓存文件。
  - Renderer 新增 `mineruCacheExistsByPdfPath` 状态，并对 Library 中的 PDF 静默探测缓存存在性。
  - Library PDF 条目名称后新增状态图标区：已解析显示绿色 `check`，仅缓存显示灰色下载图标。
  - 图标悬停支持原生 tooltip，分别显示“已解析”“已缓存”。
- 修改文件：`apps/desktop/src/main/appState.ts`、`apps/desktop/src/main/index.ts`、`apps/desktop/src/preload/index.ts`、`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/styles/sidebar.css`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：如果你想让状态更显眼，可以继续把图标换成更接近资源管理器的圆点/徽标样式，或者补“未缓存/解析中/解析失败”的更多状态。

## 19:51 - 开始扩展 MinerU Note 格式模板与清缓存能力

- 目标：让 MinerU 可配置“解析文段到 Note”的默认格式，并支持清除当前文档或全部文档的解析缓存。
- 状态：进行中
- 已完成：已重新阅读项目约定；已定位 MinerU 配置/菜单入口、MinerU 生成 Note 的主逻辑、Note 样式可选项和 MinerU 缓存文件路径。
- 修改文件：待修改
- 验证结果：待代码修改后运行 `npm run typecheck` 与 `npm run build`
- 下一步：扩展 `PersistedMineruSettingsState`，补充缓存清理 IPC，并把格式模板与清缓存操作接入 `PdfViewer` 的 MinerU 配置/菜单。

## 20:01 - 继续扩展 MinerU Note 格式模板与清缓存能力

- 目标：为 MinerU 菜单增加可配置的 Note 格式（字体、字号、加粗、倾斜、背景色）并支持清除当前/全部解析缓存。
- 状态：进行中
- 已完成：已补充 MinerU 设置模型中的 `noteStyle`；已补充主进程缓存清理 IPC：`mineru:clear-cached-result` 与 `mineru:clear-all-cached-results`；已开始将 Note 来源片段高亮改为使用 block 自身的背景色。
- 修改文件：`apps/desktop/src/main/appState.ts`、`apps/desktop/src/main/index.ts`、`apps/desktop/src/preload/index.ts`、`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/App.tsx`
- 验证结果：尚未重新运行 `npm run typecheck` / `npm run build`
- 下一步：完成 `PdfViewer` 的 MinerU 菜单 UI，接入格式模板与清缓存按钮，再跑 `typecheck` / `build`。

## 20:07 - MinerU Note 样式模板与清缓存完成

- 目标：为 MinerU 增加“解析到 Note 的默认格式”配置，并支持清除当前文档/全部文档的解析缓存。
- 状态：已完成
- 已完成：
  - 扩展 MinerU 设置模型，新增 `noteStyle`：字体、字号、加粗、倾斜、背景色。
  - MinerU 配置面板新增对应控件，调整后会持久化保存。
  - 一键生成笔记与从 MinerU 解析框拖入 Note 现在共用同一套默认样式模板。
  - Note 中来源片段的背景色改为跟随 block 样式，不再固定写死浅蓝。
  - 主进程新增清除当前 MinerU 缓存和清除全部 MinerU 缓存的 IPC。
  - MinerU 菜单新增“清除当前文档解析缓存”“清除所有文档解析缓存”按钮，并会同步刷新内存状态和 Library 状态图标。
- 修改文件：`apps/desktop/src/main/appState.ts`、`apps/desktop/src/main/index.ts`、`apps/desktop/src/preload/index.ts`、`apps/desktop/src/preload/thesis-agent.d.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/app/constants.ts`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/components/editor/EditorContent.tsx`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：如果继续打磨，可把 MinerU 配置里的样式预览做成实时预览条，或者再补“清缓存前确认弹窗”。

## 20:29 - 修复 PDF 拖入文件夹失败

- 目标：解决左侧 Library 中 PDF 无法稳定拖入文件夹的问题。
- 状态：已完成
- 已完成：
  - 在 Library PDF 条目拖拽启动时补充 `text/plain` 数据，提升 Electron/Chromium 对拖拽会话的兼容性。
  - 将文件夹整个可见区域升级为 drop shell，不再只依赖展开后的子列表区域接收 drop。
  - 保留根目录 drop zone，并同步强化文件夹高亮反馈。
- 修改文件：`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/styles/sidebar.css`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：如果你实测仍有特定情况拖不进去，我会继续补“折叠文件夹 hover 自动展开”或“更大的插入目标提示条”。

## 20:26 - 开始收口 Library 顶部按钮与空白区右键菜单交互

- 目标：让 Library 顶部按钮支持再次点击收回菜单、空白处右键可导入 PDF/新建文件夹、三个按钮 hover 时只高亮当前按钮。
- 状态：进行中
- 已完成：已重新阅读项目约定；已定位 `SidebarViews.tsx` 中的 `openSpecificMenu`、Library 头部按钮渲染和 `sidebar.css` 中顶栏 hover 联动样式。
- 修改文件：待修改
- 验证结果：待代码修改后运行 `npm run typecheck` 与 `npm run build`
- 下一步：修改按钮切换逻辑，补 Library 空白区 context menu，并调整 hover/focus CSS 只作用到当前按钮。

## 20:31 - Library 顶部按钮与空白区右键菜单交互完成

- 目标：让 Library 顶部按钮支持再次点击收回菜单、空白处右键可导入 PDF/新建文件夹、三个按钮 hover 时只高亮当前按钮。
- 状态：已完成
- 已完成：
  - Sort / Import 两个按钮改为点击切换，同一个按钮再次点击会收回对应菜单。
  - Library 空白区域新增右键菜单，支持 `Import Paper` 与 `New Folder`。
  - 顶部三个按钮的 hover / focus 高亮从“整排一起亮”改为“指到哪个亮哪个”。
  - 为 context menu 补了独立定位与关闭逻辑，不影响原有头部弹层菜单。
- 修改文件：`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/styles/sidebar.css`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：如果继续打磨，可以把空白区右键菜单再扩展成也支持“清空文件夹 / 粘贴 / 排序”等 Explorer 风格动作。

## 20:34 - 修复蓝框片段拖不进 AI 对话框

- 目标：解决 PDF 蓝框片段在部分情况下无法拖入 AI 对话框的问题。
- 状态：已完成
- 已完成：
  - 发现蓝框按钮原先只有 `region.mineruBlock` 存在时才允许 `draggable`，导致从缓存/Note 恢复出来的蓝框虽然可见，但实际上无法启动拖拽。
  - 将蓝框按钮改为统一可拖拽，并在 `PdfViewer` 中为缺少 `mineruBlock` 的区域构造兜底拖拽 payload。
  - 兜底 payload 会使用区域自身的 `pageNo / rect / label / rawType`，因此恢复出来的蓝框也能进入 AI 拖拽链路。
- 修改文件：`apps/desktop/src/renderer/components/PdfViewer.tsx`、`apps/desktop/src/renderer/components/pdfviewer/PdfViewerParts.tsx`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：如果还想继续增强，可以给蓝框拖拽时加一个更明显的“可拖入 AI”指针提示或拖拽预览阴影。

## 20:52 - 修复 createMineruNoteFromResult 初始化顺序报错

- 目标：修复运行时 `Cannot access ''createMineruNoteFromResult'' before initialization` 报错。
- 状态：已完成
- 已完成：
  - 确认 `currentPdfLinkedRegions` 的 `useMemo` 在 render 阶段提前引用了尚未初始化的 `createMineruNoteFromResult`，属于典型 const TDZ 问题。
  - 将 `createMineruNoteBlock` 与 `createMineruNoteFromResult` 调整到 `currentPdfLinkedRegions` 之前，保证依赖顺序正确。
  - 保持原有 MinerU 区域合并与生成逻辑不变，只修正声明顺序。
- 修改文件：`apps/desktop/src/renderer/App.tsx`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：如果继续整理这块逻辑，可以把 MinerU 相关 helper 再单独拆到 `renderer/app/mineru*` 工具文件里，减少大入口文件中的初始化顺序风险。

## 20:56 - 开始修复 createMineruNoteFromResult 运行时 TDZ 报错

- 目标：修复 `Cannot access ''createMineruNoteFromResult'' before initialization` 运行时错误，并确保蓝框恢复拖拽链路后不再触发初始化顺序问题。
- 状态：进行中
- 已完成：已重新阅读项目约定；已定位 `currentPdfLinkedRegions` 的 `useMemo` 在 `createMineruNoteFromResult` 之前声明，属于 render 阶段的典型 TDZ 风险；已确认蓝框拖拽修复后更容易触发这条初始化顺序问题。
- 修改文件：待修改
- 验证结果：待修复后运行 `npm run typecheck` 与 `npm run build`
- 下一步：将 `createMineruNoteBlock` / `createMineruNoteFromResult` 调整到 `currentPdfLinkedRegions` 前面，再重新验证构建与运行时链路。

## 21:06 - 修复 MinerU 框点击当前 Note 与拖入 AI

- 目标：统一 PDF 上深浅两类 MinerU/Note 蓝框的点击和拖拽语义；点击时跳到当前 Note 页正打开笔记中的对应片段，若当前笔记没有对应片段则提示；拖拽时能落入 AI 输入框。
- 状态：已完成
- 已完成：已重新阅读开发约定；已确认仓库根目录为 `E:/THESIS_AGENT`；已定位两类框来自真实 Note 来源区域与解析缓存生成区域叠加，AI 输入框 dragover 读取自定义 MIME 内容导致 drop 不触发；已将 PDF 区域按 MinerU block/坐标去重并优先保留当前打开 Note 的真实区域；点击蓝框现在只在当前打开的 Note 中查找对应片段，找不到时提示“当前笔记里没有对应片段”；AI 输入框改为 dragover 阶段只判断 MIME 类型，drop 阶段再读取 payload；PDF page 模式下蓝框统一允许拖拽。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/PdfViewer.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：建议在实际窗口里用同一页分别点击“当前 Note 有片段 / 当前 Note 没有片段”的蓝框，并把文字框与图片/图表框各拖一次到 AI 输入框确认手感。

## 21:12 - 修复跨页跨栏 MinerU 蓝框缺段

- 目标：修复同一段文字跨页或双栏换列时，PDF 蓝框只覆盖第一段而遗漏新页/新列延续文字的问题。
- 状态：已完成
- 已完成：
  - 为 MinerU 解析块新增 `segments` 多片段来源结构，保留原有单 `rect` 兼容旧结果。
  - 主进程解析 `*_model.json`，通过 `merge_prev` 识别同一段在新页或新列中的延续片段，并按模型坐标独立归一化。
  - Note 生成和拖入 Note 时会为同一 MinerU block 写入多个 `sourceRefs`，PDF overlay 会按页码与坐标生成多个蓝框。
  - 状态恢复时会清洗并迁移已有 MinerU source refs；点击延续片段也会按 `mineruBlockId` 跳到当前打开 Note 的对应 block。
  - 兼容裸 `content_list.json` / `model.json` 文件名，并在 PDF 蓝框去重 key 中加入页码，避免跨页延续段坐标相近时被错误合并。
  - 用本地 `workspace/mineru/remote-file.zip` 确认 `_model.json` 存在且可用于识别跨段片段。
- 修改文件：`packages/shared/src/index.ts`、`apps/desktop/src/main/mineru.ts`、`apps/desktop/src/main/appState.ts`、`apps/desktop/src/renderer/App.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`
- 验证结果：`npm run typecheck` 通过；`npm run build` 通过。中途因 `migrateMineruSegmentSourceRefs` 中 `mineruSourceRef` 可空收窄导致一次 typecheck 失败，已记录到 `logs/errors/2026-05-20.md` 并修复。
- 下一步：建议用真实跨页/双栏论文重新执行一次 MinerU 解析，确认新页/新列延续蓝框显示完整。

## 22:11 - 继续修复跨栏 MinerU 蓝框漏框

- 目标：根据截图复现 ORCA 论文同一段从左栏底部续到右栏顶部时，右栏续段没有蓝框的问题。
- 状态：进行中
- 已完成：已重新阅读开发前必读文档；已确认仓库根目录为 E:/THESIS_AGENT；准备检查本地 MinerU 缓存和跨栏续段在解析结果中的真实结构。
- 修改文件：待修改
- 验证结果：待定位修复后运行 
pm run typecheck 与 
pm run build。
- 下一步：搜索 ORCA 样例 PDF/缓存，确认续段是否缺少 merge_prev 或被当前清洗逻辑排除。
## 22:11 - 修复 AI 栏消息排版

- 目标：修复 AI 面板里助手回复的排版问题，避免状态行、正文和操作按钮挤在一起，导致内容错位或过窄换行。
- 状态：进行中
- 已完成：已重新阅读项目开发约定；已定位 AI 面板的消息渲染在 `apps/desktop/src/renderer/App.tsx`，相关样式集中在 `apps/desktop/src/renderer/styles/ai.css`。
- 修改文件：待修改
- 验证结果：待代码修改后运行 `npm run typecheck` 与 `npm run build`
- 下一步：把助手消息改成更明确的纵向结构，并让操作按钮从正文流里退出，避免挤压内容区。

## 22:11 - 修复 AI 栏消息排版

- 目标：修复 AI 面板里助手回复的排版问题，避免状态行、正文和操作按钮挤在一起，导致内容错位或过窄换行。
- 状态：已完成
- 已完成：已将助手消息改为“正文内容 + 独立操作按钮列”的结构；处理状态行现在只占正文列，不再和按钮互相挤压；同时补充了助手消息、状态行、正文和操作按钮的局部样式，避免窄侧栏下内容变形。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/styles/ai.css`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：如果你在真实窗口里还觉得按钮列太显眼，下一轮可以再把按钮缩成更贴边的悬浮列。
## 22:19 - 调整 AI 跳转条为等距时间轴样式

- 目标：把 AI 面板右侧的问题跳转点改成整条等距分布的时间轴样式，并支持 hover 显示问题预览、当前问题高亮。
- 状态：已完成
- 已完成：已将 AI 面板右侧问题导航从普通纵向点列改为整条轨道；问题节点现在按总数在轨道上等距分布；新增 hover/focus 预览浮层显示对应问题摘要；滚动消息列表时会自动同步当前所在问题并高亮当前节点。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/styles/ai.css`、`CHANGELOG.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：如果你实测还想更像参考图，可以继续把轨道改得更细、更暗，或把当前节点再做成更亮的环形描边。
## 22:27 - 修复侧栏中文乱码

- 目标：修复左侧栏和笔记面板中的中文乱码，恢复正常中文文案显示。
- 状态：已完成
- 已完成：已恢复 `SidebarViews.tsx` 与 `NotePanelContent.tsx` 中错误编码的中文文案；已为 AI 分支标题增加乱码兜底逻辑，若旧缓存标题疑似乱码则回退到首条用户消息摘要；已再次扫描 Renderer 侧相关模块，除乱码检测正则本身外未发现新的界面乱码残留。
- 修改文件：`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`apps/desktop/src/renderer/app/sidebarUtils.ts`、`CHANGELOG.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过；已运行 `rg -n` 扫描 Renderer 侧剩余乱码片段，除乱码检测正则本身外未发现残留 UI 文案。
- 下一步：如果你实测还有个别旧标题显示异常，可以继续补一次启动时的数据清洗，把持久化缓存里的乱码标题直接写回成正常标题。

## 22:23 - 完成跨栏 MinerU 旧缓存补框

- 目标：修复旧 MinerU 结果里同一段文字已经跨到右栏、但缓存只保存左栏 `rect` 时，PDF 蓝框漏掉右栏续段的问题。
- 状态：已完成
- 已完成：在 MinerU 结果清洗阶段增加同页双栏续段推断；当左栏底部段落文本密度异常高、下一块正文从右栏中部开始、且右栏顶部没有其它内容占位时，自动补出右栏顶部到下一块正文前的第二个 `segments` 区域；解析完成后的 IPC 返回值也改为写入缓存时清洗后的结果，保证新解析和旧缓存都能立即显示补框。
- 修改文件：`apps/desktop/src/main/appState.ts`、`apps/desktop/src/main/index.ts`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已用本地 ORCA 样例缓存模拟确认目标段落会补出右栏续框；`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：在真实 Electron 窗口重启或重新打开该 PDF 后，检查第 6 页右栏顶部续段蓝框是否出现，并确认点击仍跳到当前 Note 的对应片段。

## 22:30 - 继续修复清缓存重解析后跨栏仍漏框

- 目标：根据用户截图，修复清空 MinerU 缓存并重新解析后，第 6 页左栏底部段落续到右栏顶部仍没有蓝框的问题。
- 状态：已完成
- 已完成：已重新阅读开发前必读文档和 PDF 工作流；已确认清缓存重解析后的 `mineru_6_76_text` 仍是文本跨栏但 `segments` 为空；已将跨栏补段逻辑下沉到 `packages/shared`，并在主进程清洗、Renderer 恢复状态、读取缓存、解析完成和生成/绘制 MinerU Note 前统一调用，避免缓存文件本身缺少 `segments` 时继续漏框；同时修复 Graph 侧栏 AI 问题树根节点缺少 `focusedMessageId` 导致的类型检查失败。
- 修改文件：`packages/shared/src/index.ts`、`apps/desktop/src/main/appState.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已用真实 `6d1a7f52` 缓存调用共享规范化函数，确认 `mineru_6_76_text` 会补出第二个右栏续段 `{ x: 0.511, y: 0.088, width: 0.402, height: 0.26 }`；最终 `npm run typecheck` 通过；最终 `npm run build` 通过。
- 下一步：重启或刷新 Electron 前端后，PDF 第 6 页右栏顶部续段应显示蓝框；若当前窗口仍是旧运行时代码，需要重新启动开发/应用窗口加载新 bundle。
## 22:31 - 调整 Graph 为纯问题树并收紧 AI 标签

- 目标：让 Graph 只显示问题层级，不再显示回答节点和分支标签；同时让 AI 栏顶部标签只使用问题文本作为标题。
- 状态：已完成
- 已完成：已将 Graph 侧栏从“问题 + 回答 + 分支”结构改为纯问题树；同一对话中的连续问题会按层级串成“问题 / 子问题 / 孙问题”，分支对话则挂到对应问题下方；AI 顶部标签改为统一显示问题摘要，不再显示分支标题或对话名。
- 修改文件：`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`apps/desktop/src/renderer/app/sidebarUtils.ts`、`apps/desktop/src/renderer/App.tsx`、`CHANGELOG.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：如果你实测还想让问题树更直观，可以继续补“默认展开当前问题路径”或“问题层级连线更明显”的视觉强化。

## 22:45 - 继续修复第 7 页跨栏 MinerU 漏框

- 目标：根据用户反馈检查 ORCA 论文第 7 页同一段从左栏续到右栏顶部时仍没有蓝框的问题，扩展同页双栏补段规则。
- 状态：已完成
- 已完成：已重新阅读开发前必读文档；已按 PDF skill 渲染检查第 7 页版面；已确认 `mineru_7_88_text` 文本长 862 且只保存左栏底部 `rect`，但正文实际续到右栏顶部；已将同页双栏补段候选长度阈值从 900 放宽到 800，同时保留右栏空白检测与文本密度校验；已补齐 `SidebarViews.tsx` 中缺失的 `getChatTurns` 导入，解除旁路 typecheck 阻塞。
- 修改文件：`packages/shared/src/index.ts`、`apps/desktop/src/renderer/components/sidebar/SidebarViews.tsx`、`CHANGELOG.md`、`logs/progress/2026-05-20.md`、`logs/errors/2026-05-20.md`
- 验证结果：已用真实 `6d1a7f52` MinerU 缓存验证第 7 页 `mineru_7_88_text` 会补出右栏续段 `{ x: 0.511, y: 0.087, width: 0.402, height: 0.151 }`，第 6 页 `mineru_6_76_text` 补段保持不变；已用 `pdftoppm` 渲染第 7 页复核版面；`npm run typecheck` 通过；`npm run build` 通过。
- 下一步：重启或刷新 Electron 前端后，第 7 页右栏顶部 “For instance...” 到下一段前的同段文字应显示蓝框。

## 22:49 - 修复 note 输入卡顿

- 目标：让 note 编辑输入更顺滑，减少每次按键引发的整页重渲染、深拷贝和全量字符串比较。
- 状态：已完成
- 已完成：已让 note block 更新保留未变更块的引用；已把 note 标题与正文输入改成局部草稿并通过 `startTransition` 低优先级提交；已把 note 历史记录从深拷贝快照改成引用快照，避免每次输入重复克隆整篇笔记；已把 note block 组件做成 `memo`，让未变更区块跳过重渲染；已顺手把 note 面板的全局键盘监听 effect 去掉了会跟随 note 变化的无关依赖。
- 修改文件：`packages/notes/src/index.ts`、`apps/desktop/src/renderer/app/notePanelUtils.ts`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：建议实际打开一篇 block 较多的笔记，连续输入几段文字确认光标和滚动是否明显更顺。
## 22:57 - 为 AI 面板接入公式渲染

- 目标：让 AI 回答中的 LaTeX 公式在面板里以编译后的数学公式显示，而不是只显示原始源码。
- 状态：已完成
- 已完成：已为 AI 面板的 `react-markdown` 链路接入 `remark-math` 与 `rehype-katex`；AI 回复中的行内公式和块公式现在会走 KaTeX 渲染；同时补充了 AI 回复区域中 `katex-display` 的滚动与间距样式，避免长公式把面板挤坏。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/styles/ai.css`、`apps/desktop/package.json`、`package-lock.json`、`CHANGELOG.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：如果你实测发现某些 AI 返回的公式仍没有被识别，下一轮可以继续兼容更多写法，比如 fenced code 里的 `latex`、`\\[ ... \\]` 外层多余转义，或模型输出中混合中文说明与公式的特殊格式。

## 23:08 - 调整历史回答处分支与分支对话折叠

- 目标：在过往回答上创建分支时，自动把原回答之后的既有后续整理成一条旧分支；新分支对话中折叠分支前历史，只保留所选回答，并在顶部提供“以往对话已折叠”跳回入口。
- 状态：已完成
- 已完成：已增强 `branchAiConversationFromAnswer`，当在主链历史回答处分支且后面还有既有消息时，会先把原后续自动提炼成一条同锚点的旧分支，再创建新的空白分支；已为分支对话补充分支锚点计算和顶部蓝色折叠提示；点击提示会切回原对话。
- 修改文件：`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/styles/ai.css`、`CHANGELOG.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：如果你想继续收紧体验，可以再把“回到原对话”做成直接定位到所选回答，而不是只切回原会话。

## 23:13 - 修复 MinerU 笔记空行退格删除

- 目标：让 MinerU 生成的 note 文段在块首按 Backspace 时能够删除空块或合并前置空行，避免中间空白段落卡在笔记里删不掉。
- 状态：已完成
- 已完成：已确认 MinerU 笔记是按 block 组织而非单纯 textarea 换行；已在 note 面板补充块级 Backspace 处理，空块会直接删除，块首遇到前一个空块时也会删除前置空块；已新增 note 文档块删除能力，Renderer 不再直接改写数组删除逻辑。
- 修改文件：`packages/notes/src/index.ts`、`apps/desktop/src/renderer/App.tsx`、`apps/desktop/src/renderer/components/note/NotePanelContent.tsx`、`logs/progress/2026-05-20.md`
- 验证结果：已运行 `npm run typecheck` 通过；已运行 `npm run build` 通过。
- 下一步：建议在真实窗口里试一下 MinerU 解析出来的空段落和普通空块，确认块首 Backspace 的删除手感是否足够直观。
