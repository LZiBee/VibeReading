# packages 目录

本目录放可复用功能包和核心业务模块。

模块划分：

- `workbench`：工作台内核。
- `ui`：通用 UI。
- `pdf`：PDF 阅读和标注。
- `notes`：结构化笔记。
- `ai`：模型接入。
- `rag`：检索问答。
- `graph`：知识图谱。
- `citations`：参考文献。
- `db`：数据库和索引。
- `shared`：通用类型和协议。

新增功能时先判断模块归属，不要把业务逻辑堆在应用入口里。

